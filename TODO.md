# TODO

Items to address in a future release.

## UI

- [ ] Change seek bar marker tooltip font size to 12px (`text-xs`) in both coach and teacher app (`VideoPlayer.tsx`)

## Distribution

- [ ] macOS code signing and notarization — eliminates the "damaged and can't be opened" Gatekeeper warning for end users without requiring the `xattr -cr` workaround
